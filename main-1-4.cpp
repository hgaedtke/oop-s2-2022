#include <iostream>

extern void copy_doubles(double *old_array, double *new_array, int length);

int main() {
    double old_array[3] = {1, 2, 3};
    double new_array[3];
    int length = 3;
    return 0;
}